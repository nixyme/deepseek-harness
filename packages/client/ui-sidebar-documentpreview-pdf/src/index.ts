/**
 * Lazy PDF renderer plugin, node half. Pure UI plugin: the empty apply exists so
 * the lazy package can occupy a Loader row while its browser half stays dormant.
 */

/** Host plugin body - no host-side behavior for this renderer. */
export function apply(): void {}
